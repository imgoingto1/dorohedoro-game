# Only files listed here are tracked; everything else in the local project folder
# (place/model exports, captures, tools) stays local. This repo is public.
/*
!/.gitignore
!/README.md
!/docs/
!/art-direction/
# Reference images (anime/manga screenshots) stay local in /reference/ - this repo is public.
